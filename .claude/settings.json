{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.py) uv run ruff format \"$f\" 2>/dev/null || true;; esac; }",
            "timeout": 15,
            "statusMessage": "Formatting Python file..."
          }
        ]
      }
    ]
  }
}
